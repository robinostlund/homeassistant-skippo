# pytest.ini — for lightweight tests (tests/test_*.py)
# HA-style tests (tests/ha/) run inside the HA core tree with its own pytest config.
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
